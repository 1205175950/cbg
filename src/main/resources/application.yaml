server:
  port: 9999

spring:
  datasource:
      url: jdbc:mysql://localhost:3306/cbg?useSSL=false&serverTimezone=UTC
      username: root
      password: 123456
      driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mappers/*.xml  # Mapper XML文件位置
